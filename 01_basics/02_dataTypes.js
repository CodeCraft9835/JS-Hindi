"use strict"; // it means treat all JS code as newer version

//alert(3+3); it shows error while running , reason we are curremtly using node.js , not a browser
console.log(3+3)

console.log("Hitesh") // Note kindly take care of your code structure it should be beautiful so that the readability should be high
 
let name = "apurva"
let age = 19
let isLoggedIn = false

// number => 2 to power 53 range of values
//  bigint for larger values range than number
 //string => ""
 //boolean => true/false
 // null => standalone value it means empty  its an object 
 // undefined =>  abhi value assigned nahi hue hai
 // symbol  =>  it used to define uniquness


 // object
console.log(typeof undefined); // Output : undefined  because its itself an type
console.log(typeof null); // Output : object
